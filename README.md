### 一键执行脚本（推荐）
直接复制以下命令到服务器终端，回车即可自动配置 swap：
```bash
wget -q https://raw.githubusercontent.com/NTPlay4/Swap-Config/refs/heads/main/swap-config.sh -O swap-config.sh && chmod +x swap-config.sh && sudo bash swap-config.sh
